# GitHub Personal Access Token
# Create a token at: https://github.com/settings/personal-access-tokens
# Required scopes: repo (for private repos) or public_repo (for public repos only)
# Also used for GitHub Copilot API access
GITHUB_TOKEN=your_github_personal_access_token_here

# Optional: GitHub API base URL (for GitHub Enterprise)
# GITHUB_API_URL=https://api.github.com

# Optional: Request timeout in milliseconds (default: 30000)
# REQUEST_TIMEOUT=30000

# Optional: Maximum file size to analyze in bytes (default: 1MB)
# MAX_FILE_SIZE=1048576

# Optional: Enable debug logging (default: false)
# DEBUG=false 
